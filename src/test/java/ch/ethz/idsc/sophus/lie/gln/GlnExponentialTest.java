// code by jph
package ch.ethz.idsc.sophus.lie.gln;

import junit.framework.TestCase;

public class GlnExponentialTest extends TestCase {
  public void testSimple() {
    // Tensor err = RandomVariate.of(NormalDistribution.of(0.0, 0.1), 5, 5);
    // Tensor exp = GlnExponential.INSTANCE.exp(err);
    // Tensor log = GlnExponential.INSTANCE.log(exp);
    // Chop._07.requireClose(log, err);
  }
}
